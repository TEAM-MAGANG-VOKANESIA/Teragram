import: 
  - recipe/laravel.php
  - contrib/npm.php

config:
  repository: 'git@github.com/TEAM-MAGANG-VOKANESIA/Teragram.git'
  writable_mode: chmod
  remote_user: deployer

hosts:
  dev:
    hostname: latihan.vistech.co.id   
    deploy_path: '~/Teragram'
tasks:
  build:
    - cd: '{{release_path}}'
    - run: 'npm run build'

after:
  deploy:failed: deploy:unlock
